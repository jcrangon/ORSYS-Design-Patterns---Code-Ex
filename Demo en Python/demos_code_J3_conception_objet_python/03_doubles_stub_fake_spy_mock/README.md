# Démo 03 — Stub, Fake, Spy, Mock avec de vrais tests pytest

Commande : `python -m pytest -q`

La classe de test contient quatre doubles manuels simples.

Message pédagogique : **un double est un rôle. Tout n'est pas un mock.**
